{
	"name": "@forms/form",
	"version": "0.0.1",
	"type": "module",
	"scripts": {
		"dev": "rsbuild dev --open --port 5174",
		"build": "rsbuild build",
		"preview": "rsbuild preview",
		"bundle-analyzer": "BUNDLE_ANALYZE=true pnpm run build",
		"rsdoctor": "RSDOCTOR=true pnpm run build",
		"deps-check": "pnpx depcruise src --include-only '^src'"
	}
}
